# Build 19 Checkpoint G — synthetic batch staging corpus

Not Andrew’s library. Controlled synthetic tokens only.

Regenerate with:

```bash
cd runtime && npm run knowledge:fixtures-g
```

Contains mixed-format intake for batch preflight, dry-run, execute, recovery, and retrieval proofs. Batch manifests are operational provenance only — never evidence.
